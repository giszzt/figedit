opencv-python>=4.9   # structure detection (4.x and 5.x both supported)
paddleocr>=3.7       # text detection (PP-OCRv6)
paddlepaddle>=3.3    # PaddleOCR backend
numpy>=1.24
Pillow>=10.0
scipy>=1.10          # chroma key edge analysis
matplotlib>=3.7      # formula rendering (math_renderer)
latex2mathml>=3.81   # LaTeX -> Office Math conversion
lxml>=5.0            # SVG/XML processing
# Optional: cairosvg improves PPTX media rendering quality (guarded import; svglib+reportlab is the fallback)
