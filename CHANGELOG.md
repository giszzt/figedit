# 更新日志 / Changelog

## 0.9.0 — 2026-08-09

拆了 8 个真实任务的完整记录后得到一条硬事实：**墙钟时长约等于工具往返次数乘以每次 30 秒，脚本计算量占总时长不到 2%**（合成一次 SVG 7.6 秒，切图 0.29 秒，整个任务的脚本计算加起来不到一分钟）。所以提速只有一条路——减少往返次数，"让脚本更快"物理上不可能有效果。耗时第一名是模型临时手写 Python 数像素，占 41%，8 个任务里无一例外都排第一。

### 新增：`measure.py` 批量量尺寸

八种查询一次问完：`info` `bbox` `color` `clearance` `alpha-bbox` `fontfit` `diff` `zoom`。返回一张数字表加一张带标签的放大拼图，`bbox` 的量得结果用红框画在拼图上，看一眼就知道量对没量对。**一次问十个和问一个花的时间一样**，历史上三五十次的往返压成三五次。`zoom` 查询替代"自己裁图存盘再 Read"那条老路。

`--exclude-text` 排除窗口内的文字像素。实测在交错面板图上发现，粗窗口几乎总会把图标旁边的说明文字圈进来（"Calvin's"、"init"、"as-Judge Filter"），不排除量出来的是"图标加标题"的外接矩形，不是图标。

### 新增：每个入口脚本打印结论

`snap_boxes` 原本只打印 `{"counts":...}`，判定结果全在文件里，模型只能再写代码解析回来——08-07 那个任务里同一条解析命令原样跑了 5 次。现在 `snap_boxes` 打印判定不通过的那几行，`prepare_measurements` 打印画布尺寸、OCR 条数、面板与文字槽数量、低置信文字清单（原本是把整个 JSON 倒出来），`fit_text` 打印溢出量表，`check_plate_registration` 打印缩放比、偏移与是否通过。

画布尺寸打印顺带干掉了每个任务开头那次 `im.size, im.mode` 的往返。简单图的管线外调用里，读自己脚本的输出占了一半以上，这一条对简单图的收益比对复杂图更大。

### 新增：差异工单 `fix_worklist.py`

`visual_compare_qa` 报的是 12×12 网格里最差的格子，模型拿到"(340,880) 那格色差 22"之后还得再查那格里是哪个元素，等于又一次往返。现在把色差图投影到每个元素的框上，产出按误差排序、点名到元素 id、带毛病类型（位置偏了 / 字号偏大偏小 / 画宽了 / 颜色不符 / 整个没画出来）的工单，外加一张误差最大的十二个元素的源图-成品上下对照图。由 `compose --stage svg` 自动运行，成本 1.0 秒。

门槛取"全图基线 ΔE 的 2 倍"与 12 的较大者。用基线做参照是因为重打字、重画形状的图整体基线本来就高，固定阈值在这类图上会刷屏，把真正的缺陷埋掉。

### 新增：`tests/task_cost_report.py`

从会话记录统计一个任务的往返次数、即兴测量次数、看图次数与分钟数。管线内部的计数器看不到绕开管线的调用，而那恰恰是要消灭的部分。下一轮评估改动有没有效果，跑这个脚本，不要再翻聊天记录。

### 未通过验收，已停放：`read_figure.py`

给图上每个对象编号、让模型点编号而不打坐标的方案。32 个历史任务 2047 个真值对象上实测：不设候选上限时能指到 85.0%（候选最多 1946 个，标号图没法看），压到 220 个候选时 75.8%，门槛是 99%。根因是连通域会把画面丰富的具象对象打成碎片——照片式徽章那类图整张只有 5.4%，而扁平纯色图标的图能到 100%。试过用 `probe_geometry.image_profile` 的 `flat_design_score` 做弃权判断，32 张图一张都没触发，因为那个分数量的是背景平整度，量不出对象是不是具象的。

脚本与失败记录在 `parked/`，评测脚本 `tests/eval_read_figure.py` 保留，改完直接重跑对照。**不要因为它看起来快完成了就接线**：76% 的能指到率意味着模型看完标号图仍有四分之一的对象要手工量，那比不给标号图更慢，因为多花了看图的时间。

### 未动

分阶段 compose、19 个质量门、可编辑性审计、PPTX 导出与语义级顶层解组、清版底管线、色键再生管线及其撞色保护、四来源指派、重绘门槛、验证分档、`manifest_edit` 的全部能力。四个阶段的名字仍然是勘察 → 备料 → 组装 → 验收。

## 0.8.0 — 2026-08-06

0.7.0 之后的实跑审计给出一个明确结论：瓶颈只有一个，是 **manifest 撰写本身没有任何自动化辅助**。最近两次任务的 manifest 分别是 255 和 235 个元素，而 `prepare_measurements.py` 产出的 `draft_manifest.json` 里 `elements` 恒为空——OCR 找到 117 个文字候选，模型仍要从零手写 124 个 text 元素，121 个 rect 的坐标全靠自己算连通域。路由、裁剪、生成、验证都是它的外围。这一版只做三件事：把 manifest 撰写自动化、把文档负担降下来、把脚本表面收敛。判据、质量门、能力边界一条没动。

### 新增：`probe_geometry.py` 结构证据

固化模型此前每任务手写 29–33 个临时测量脚本才能得到的东西，`prepare_measurements.py` 默认调用，产出 `work/geometry.json` 与 `diagnostics/geometry_overlay.png`：

- `fill_regions`：面板/卡片的 bbox、填充色、描边色、圆角估计、嵌套关系。同时识别**实心面板**和**描边空心面板**——后者在源图里是一个环形连通域，不是四条边，这是找到论文插图里那些"浅底加细边框"面板的关键。实心面板按它**围出的形状**判定，因为面板里坐满图标和卡片时，纯色连通域本身是破碎的。
- `text_slots`：OCR 行的槽位、字号估计、文字色。字号比例（拉丁 0.875、中日韩 0.758）与取色方式（离底色最远的那 20% 像素取均值）都是在 26 个历史任务语料上标定出来的。

**该沉默时沉默。**先算 `flat_design_score`，照片/插画/海报类低于阈值时 `abstained: true`，只输出极少量高置信候选。错误候选进入 manifest 的代价远高于模型自己量一次。

**不做分隔线检测。**建过，在语料上实测 836 个候选只有 5 个能被证实（精确率 0.002）：每个面板边缘的抗锯齿过渡带都自成一个细长连通域，与发丝分隔线无法区分。这是本轮的"箭头检测"，按 1.0 的教训直接删掉，而不是留着降低门槛。

### 新增：`draft_elements.py` 元素草稿

把 `geometry.json` 变成可直接采纳的 manifest 元素。三条约束：草稿写进独立文件，绝不直接进 `manifest.json`；每项带 `provenance` 与 `confidence`；**采纳后必须立即 compose 一次并看差异热力图**——逐条读 120 个草稿的 JSON 比手写还慢，看一张合成图错的地方自己会浮出来。

`raw_detector_import` 质量门改为先查 `provenance`：交付的 manifest 还带着草稿标记，就是草稿没被看过。

### 新增：`manifest_edit.py --adopt / --patch`，生成器合法化并强制退场

0.7.0 的软禁令导致两头不靠——模型既不敢用生成器，又不用 `manifest_edit`（实跑调用 0 次）。现在：**首版允许用一次性生成器产出**，250 个元素手写 JSON 不合理；**首版落盘后生成器立即改名 `*.py.retired` 退场**，后续修改一律走 `manifest_edit.py`。

### 新增：`prepare_measurements.py --init` 任务脚手架

一条命令建目录、拷源图、写含 canvas 尺寸的 manifest 骨架，省掉实跑中 6 分钟手工目录操作。

### 变更：文档 16 → 10

删 `taxonomy.md`、`workflow.md`（四次会话零读取，且已被 SKILL.md 取代）。合并 `asset_preservation_policy.md` → `element_decision_matrix.md`、`contaminated_asset_recovery.md` → `asset_extraction.md`、`text_layer_policy.md` → `background_reconstruction.md`、`ai_clean_plate_prompting.md` + `image_backend_policy.md` → 新的 `image_generation.md`。

**判据一条不删**，且不靠承诺：合并前后机械抽取全部规范性语句（必须/不得/禁止/只有…才/应当及英文对应）逐条比对，118 → 110 条，减少的 8 条全部是被合并掉的重复表述，措辞变化的 5 条已逐条补回。

### 新增：`references/scripts.md` 接口卡

每个入口脚本一张卡：用途、命令行、输入输出格式、注意事项。直接针对实跑中每任务 29 次 `sed -n` 翻脚本源码。卡片里写明各项**实测可靠度**，避免把候选当结论。

### 新增：`tests/eval_geometry.py` 语料评测

在 29 个"源图 + 最终 manifest"配对样本上离线评测，无需模型参与。

**必须说明的评测局限**：最终 manifest 记录的是**路线选择**而不是图像几何。29 个样本里有 11 个一个面板都没画（那些区域走了整块保留或 AI 清版），所以在这些样本上，一个正确的面板候选是**无法被证实**，而不是错的。因此每类候选只在"manifest 确实用了这类元素"的样本上计分，其余计入 `unscoreable`，绝不计为通过。

**本版未通过 0.8.0 方案 §6.1 的 0.80 精确率门**：实测加权 0.471（fill 0.566、text_slot 属性 0.446）。弃权门通过。门槛保持 0.80 不下调，失败原样暴露在评测输出里。

### 回归

29 个语料样本旧 manifest 重新合成，`preview.png` 逐像素相同，svg 阶段 16 个 gate 状态与基线一致；单元测试 16 项全绿。

## 0.7.0 — 2026-08-05

这一版把判断和执行彻底分开。0.6.0 虽然消掉了逐对象量测的诱因，但勘察阶段仍要求写出带 `bbox` 的对象清单——一张 25 个素材的复合图，等于在任何脚本跑起来之前先手工产出一百多个像素数字。坐标要求本身就是测量任务，它把"一眼看出来"的判断又拖成了逐对象估框。同时第二阶段命名为"吸附"，只对从源图裁素材那一条路成立；一张连续背景图走清版加前景再生，素材根本不来自源图矩形，这个阶段名对它是空的。

### 变更：勘察产出重建路径概要，零坐标

- 第一阶段的唯一交付物改为**重建路径概要**：纯文字、无坐标、二十行以内、六个固定槽位（图型与分区 / 需要生成的 / 不可重画的对象 / 公式 / 工作量重心 / 需要用户定）。对象用名字和空间关系指认，不写 bbox。
- 概要同时是自检。写不出"工作量重心"说明没看懂图；"不可重画的对象"分不出两堆说明污染扫描没做。
- 坐标推迟到备料阶段。此时路线已锁，点粗框是纯执行动作，不会再引发路线摇摆。`work/inventory.json` 降级为喂给 `snap_boxes.py` 的执行输入，只含走切或再生的对象；纯清版压平的图可以完全没有这个文件。
- `reconstruction_plan` 降级为写 manifest 时记录的结论存档，不再是第一阶段的门。字段与校验未变，`open_questions` 阻塞仍作兜底。

### 变更：四阶段改名，第二阶段按路线分支

- 主流程定为**勘察 → 备料 → 组装 → 验收**。"吸附"从阶段名降为备料下的一个分支。
- 备料按概要指派分支，**没指派到的分支整段跳过**：纯 SVG 图不跑任何生成，纯清版压平图不跑任何裁剪与吸附。以后新增获取方式（自动候选检测、描摹）也是往备料里加分支，骨架不动。
- 新增骨架依据：输出画面上每一块像素只有四个来源——SVG 画的、从源图切的、AI 生成的、整块原样保留的。路由就是给每部分指派来源，每部分有且只有一个。这个框架不依赖任何具体工具。

### 变更：找回初代版的可识别性

- 新增**图型速查表**六行，让模型对着熟悉的图型一步认出路径，不必每次从抽象判据现场推导。这是初代版第 2 步五种图型的做法，0.6.0 换成抽象原则后丢失。
- 关键改进是**按区域查，不按整图查**。初代版一张图只能选一条路线，(a)(b) 平色、(c) 地图的复合图会被整体误判；现在各区域各查各的。
- 对象判定从"四问决策树逐个走"改为**素材两堆一次扫完**：窗口干净的切，被压盖的按需不需要单独动分再生或压平。四问降级为判据说明，不再是流程框架。

### 变更：解除 OCR 阻塞

用户确认前的禁令收窄为计费生成、裁剪与合成。OCR 不计费、输出无论走哪条路都要用，改为开头无条件运行，可与提问并行。`background_reconstruction.md` 与 `quality_checklist.md` 中的对应表述同步收窄。

### 回归

本版只改编排与表述，未动任何脚本、判据、schema 与质量门。交错面板图任务重跑，16 个 SVG 阶段 gate 状态与基线一致，`preview.png` 逐像素相同；单元测试 16 个通过。五类样例桌面演练通过：纯流程图与截图型不触发生成，海报清版不触发裁剪与吸附，五类均不产出坐标。

## 0.6.0 — 2026-08-05

这一版的主题是把体力活从模型手上拿走。0.5.0 的提速改造上线后实测无效（两次任务活跃时间约 170 分钟，与改造前持平），根因不是规则写得不够细，而是规则在对抗结构性需求：模型需要像素坐标却没有自动量测手段，只能逐个图标裁图、放大、量边界；299 元素的 manifest 没有批量修改通道，只能现场写 patch 脚本。本版用工具替换这两处禁令。

### 新增：粗框吸附（snap_boxes.py）

- 模型在勘察阶段只给一眼精度的粗框（允许 10–20px 偏差），`snap_boxes.py` 批量吸附出紧裁剪窗、量四边净空、检测窗内异物与余量均匀度，机械输出 `clean / clean-on-fill / contaminated / snap-failed` 判定和一张着色总览拼图。
- 背景色取自候选窗外的环带而非工作窗边界，避免卡片交界处混色污染墨水掩膜；`--exclude-text` 接 OCR 结果，防止相邻标题被吸进对象；抗锯齿碎片按尺寸上限桥接回主体；紧框边缘触到邻居的行列自动回缩。
- 明确废除“裁剪窗判定不得有前置分析脚本”的旧教条。语义（对象是什么、要不要独立存在、污染后改走哪条路）仍归模型，像素归脚本。`quality_audit.py` 的事后裁剪窗核验保留为兜底，判据未变。
- 交错面板图任务重放：26 个对象 24 个判定与人工结论一致，污染零漏报；random 任务 15 个中 12 个一致，误判全部偏保守。

### 新增：manifest 批量修改通道（manifest_edit.py）

- `--set` 按 id 批量赋值与数值增减，`--select` 按字段选择器批量改，`--apply-snap` / `--apply-fit` 把吸附结果和文字拟合结果直接回写，`--ops` 走文件做大批量操作，`--dry-run` 预览。
- 每次运行自动备份并在写入后重跑 `validate_manifest.py`，校验失败自动回滚。目标 id 不存在即报错，不静默跳过。
- 修复阶段不再需要现场写 `patch1.py … patch4.py`：一轮修复一条命令。

### 变更：重建计划取代 Route Decision v2

- manifest 顶层 `route_decision` 更名 `reconstruction_plan`，字段从十余个收敛到六个：`edit_scope`、`background_regions`、`validation_tier`、`open_questions`、`closeup_ids`、`inventory`。
- 废除 `schema_version`、`route_status`、`base_strategy`、`asset_groups`、`separability`、`observed_overlap`、`foreground_inventory`、`region_accuracy`、`unresolved_decisions`。素材路由的载体改为 `work/inventory.json` 对象清单（id、粗框、kind、route、理由、是否需用户决定），validator 交叉检查 `assets[].decision` 与清单 `route` 一致。
- 阻塞状态由 `open_questions` 非空表达，不再单设状态字段；非空时 manifest 不得含 assets 或 background_plans，裁剪器与 compose 硬停止。
- 旧 `route_decision`（含 v2 结构）继续兼容校验并打印弃用提示；同一 manifest 不得同时含两者。两个 example 模板和 JSON schema 已迁移。

### 变更：术语与信息架构

- 主流程定名“勘察 → 吸附 → 组装 → 验收”。“整图总判 / Global Reconstruction Read”改称“首图勘察”，`global_routing.md` 重写为 `routing.md`。
- SKILL.md 从 224 行压到 116 行，新增**重绘门槛**并置于正文而非选读文件：只有“三五笔基础图元能画得像”的图形才允许 `redraw`，有定制轮廓、渐变、阴影、多色细节或品牌身份的一律按素材处理。此前该判据只存在于按需加载的 `element_decision_matrix.md`，导致不加载 reference 的 Agent 把图标全部划为重绘。
- `routing.md` 给出对象级四问决策树（画得像吗 → 窗口干净吗 → 需要独立存在吗 → 在清版区吗），替代抽象的三轴模型。
- 用户提问合并为勘察结束时一次问完：前景深度、生成预算和 PowerPoint 附着预授权同批提出，避免验收阶段才请求授权造成长时间阻塞。

### 回归

- 交错面板图任务全量重跑：19 个 quality gate 状态与基线逐项一致，`preview.png` 与基线逐像素相同。
- 单元测试 16 个通过（新增 9 个覆盖重建计划校验、阻塞语义、清单一致性）。

## 0.5.0 — 2026-08-04

本版把全局二选一路由升级为 Route Decision v2 的区域/素材组合路由，修复复杂图中“明知有污染仍全量裁剪”和“局部连续背景未确认编辑深度就整区栅格化”的决策错误。

- 整图总判改为确定性 SVG 基底、局部/全图背景 scope 与素材组的组合决策；复杂度和体裁不再直接决定路线。
- 新增负向污染扫描：先看轮廓遮挡、外接矩形异物、边框/阴影/纹理相交和对象缺口，再允许 `clean / clean-on-fill`；污染对象直接再生、压平或阻塞，不先逐项裁图试错。
- `asset_groups` 新增 `separability` 与污染证据 `observed_overlap`；每个非背景位图素材必须被分组，crop 组与最终 `crop_window` 必须一致。
- 局部或全图连续场用 `background_scopes[]` 表达；需要编辑前景时走区域 `ai-clean-plate`，整区 `source-preserve-region` 只有用户明确接受时才合法。
- 用户未表达连续场前景深度时写入 `pending-user-choice / needs-user-input`；裁剪器和所有 compose 阶段会硬停止，避免在确认前继续处理。
- 新增区域 `background_plans[]` schema、校验、质量审计、底板放置、掩膜 scope 选择和诊断产物命名；旧单一 `background_plan` 继续只读兼容。
- 更新示例、请求模板、README 与路由测试，覆盖污染素材、待确认连续场和局部清版计划。

## 0.4.0 — 2026-08-04

本版重构执行编排，目标是在不降低信息完整性、素材保真、公式可编辑性和背景质量的前提下，减少模型重复看图与无条件 PowerPoint 验证。

- 新增 Global Reconstruction Read：先用一次整图判断锁定编辑深度、常规/AI 路线、元素策略、异常项和验证档位；局部图只补未决证据。
- 裁剪与再生改为批量覆盖、异常驱动复核；新增 `inspect_regions.py`，每页最多汇总 6 个异常区域，不再默认逐资产反复裁剪和看边界。
- SVG 成为主验收对象；PPTX 原生渲染仅由公式或静态结构风险触发。`render_pptx.py` 检测到用户正在使用 PowerPoint 时默认跳过，附着模式也只关闭脚本自己的只读 Presentation。
- `compose_svg_package.py` 支持 `svg`、`pptx`、`package` 三阶段，省略 `--stage` 时保持旧的全量行为；新增 `timings.json` 记录真实耗时和重型验证次数。
- 增加字体变量解析、箭头 marker 词汇、连接线净空、文字拟合与静态回流审计、裁剪告警候选收窗，以及新的路由和 manifest 兼容字段。
- `SKILL.md` frontmatter description 采用“能力与产物 + 支持特性 + 当用户……时使用”的通用表达；正文不重复设置“适用/不适用”，也不引入额外的“规则强度”栏目。

## 0.3.0 — 2026-08-03

这一版的主题是堵住实战中反复出现的静默缺陷和冗余计费调用，同时全面中文化。SKILL.md 和全部 references 改写为中文（标识符、字段名、脚本名保持英文）。

### 新增：裁剪窗检查（Crop Window Check）

- 常规路线的坐标裁剪新增一道肉眼判定子门：裁剪矩形窗口里除元素自身还有没有别的东西（下方的承载卡片/描边框，或不规整外接矩形圈进的邻居）。三档判定 clean / clean-on-fill / contaminated 写入资产的 `crop_window` 字段；contaminated 先试收窗，收不动走再生。判定靠看图完成，不跑前置脚本。
- `quality_audit.py` 新增 `crop_window_consistency` 门做事后像素核验兜底：边框环颜色聚类抓承载层污染，ink 触边抓切边/邻居入侵，与声明矛盾时报 review 附证据；contaminated 仍裁剪直接 failed。
- `contaminated_asset_recovery.md` 的触发条件由主观自评改为客观判定结果。

### 修复：键色安全判据统一到色相

- 根因：`probe_palette` 用欧氏距离（≥90）判安全，`chroma_key` 却按色相余弦（>0.90）删除像素——深绿距纯绿键 131 被判安全，键控时照样被毁，且报告零告警。
- `probe_palette.py` 新增 `--boxes` 逐元素色相撞色检查与最少张数的分 sheet 方案；走再生路线必须在首次生成前跑。键色默认配对：暖色配绿键，绿/紫配洋红键。
- `chroma_key.py` 报告新增逐连通域 `component_hue_drift`（键控前后主色对比，按原图内容找连通域，元素被整体删除也能报出），色相漂移 >25°、脱色或删除都进 warnings。修正了脚本内"probe_palette 保证色相分离"的虚假前提注释。
- despill 算法本身不动：上游分区挡住撞色，下游报告兜底，链条闭合。

### 修复：单张 sheet 硬约束与固定画幅补偿

- 再生 sheet 拆分只能发生在已生成一张全量 sheet 并观察到失败之后；数量、尺寸、面板分组等先验估计一律不是拆分理由。色相分区和缩微复现失败是两个明确的合法出路。
- `image_backend_policy.md` 新增 Fixed-Aspect Backends 一节：恒定画幅后端会重新取景（非拉伸），补偿方法是参考图非等比拉伸 + 提示词照抄拉伸几何 + 结果压回。新增 `check_plate_registration.py` 量化配准（scale/offset/IoU 暴力搜索），通过标志 scale ≈ 1.00 / offset ≈ 0。
- 后端失败处理按实测修正：任何失败先原地重试 1 次（存在瞬时 400/SSL EOF），重试后仍 4xx 才归为参数错误，连续 3 次失败才下沉后备。
- 前景深度决策检查点要求呈现具体计费调用数预算；交付说明含「预算/实际」对照。

### 修复：质检门失效

- editability 门新增第三态 `unavailable`：OCR 证据缺失时不再判 ok。`measurement_workspace` 字段写入文档并由 `prepare_measurements.py` 自动生成；`compose_svg_package.py` 字段缺失时回退搜索 `work/`、`../work/`，找不到显式告警。
- `slice_grid.py` 新增 `--cells` 模式：多部件图标（终端+刷新箭头、锤头+底座）被连通域切碎时，按已知格子框区域裁剪加 alpha 收边重切。

### 移除：OpenCV 依赖

- 删除 `detect_primitives_cv.py`（通用检测器，实测召回个位数）、`infer_assets.py`（实验性，无引用）、`generate_diagnostics.py`（仅消费检测器输出）。
- `slice_grid.py` 的连通域分析移植到 `scipy.ndimage`（精确等价物），cv2 依赖整体清除。
- `opencv_detector_noise` 门改名 `raw_detector_import` 并泛化为任何检测器；报告移除 OpenCV 状态行。
- SVG 绘制顺序文档修正为与代码一致（connectors 在 sections/icons 之前），并建议元素显式写 `layer` 字段。

## 0.2.0 — 2026-07-09

这一版的主题是背景感知重建。0.1.0 能处理的图有一个共同前提：背景要么是纯色，要么可以从原图干净地裁下来。文字压在照片上、图标嵌在插画里、标注散布在渐变场景中的图，此前只能整图裁切放弃编辑性。0.2.0 把这类图纳入了重建范围。

### 新增：AI 清版底板路线

- **背景判定门（Background Gate）**。在结构、文字、公式、资产四个元素门之后新增一道判定：当前景内容嵌在照片、插画、渲染场景、不规则渐变等连续视觉场里，裁切加简单矢量无法忠实还原背景时，改走清版路线。判定依据是背景可恢复性，与图的类型和视觉密度无关。
- **清版底板生成**。由参考图模型对源图做编辑式生成：擦除待重建的前景，补全被遮挡的像素，其余背景保持原样。产出一张作为视觉底层的完整底板，可编辑的文字、公式和标记叠加其上。重新设计画面的候选图会被拒绝重生成，底板必须是原图的编辑而非新场景。
- **用户路线指令优先**。两条路线默认由背景判定门分流，用户明确指定路线时以用户要求为准，且可以双向交叉：常规图可以为了提取精度主动走清版路线，带照片背景的图也可以在知晓保真代价后坚持常规裁切。单个资产同样可以按用户要求在裁切与再生之间切换。指令来源记入 manifest（`route_decision.source`），质量验收标准不因用户指令放宽。
- **前景深度决策**。清版前必须确定前景处理模式：全部图形对象提取为独立资产（full-extract）、只提取用户指定对象（selective）、或全部保留在底板中压平（flatten）。用户未明确表态时，Agent 会带着对象清单、各模式的代价和推荐意见停下来确认，再发起生成。

### 新增：色键再生管线

- 被标注遮挡、与背景纠缠、或无法干净裁切的图形对象，改由参考图模型在色键底色上重新生成，再自动抠出为透明 PNG 资产。新增三个配套脚本：`probe_palette.py` 按源图配色选取安全的色键颜色，`chroma_key.py` 去除色键底并净化边缘、投影和封闭孔洞，`slice_grid.py` 按连通域把整版再生图切分为独立资产。
- 全部前景对象合并到一张再生版上一次生成，重复出现的元素只再生一次、按共享 ID 多处放置。
- 明确禁止用 GrabCut、差分、阈值、rembg 等即兴抠图脚本替代这条管线，两条资产获取路径（坐标裁切、再生色键）各司其职。
- 新增污染资产恢复策略文档，界定何时修补裁切、何时转入再生。

### 新增：图像后端体系

- 后端按序回退：Agent 内置图像工具优先（如 Codex `image_gen`），其后依次为 Labnana GPT-Image-2、Labnana Gemini / Nano Banana、OpenAI 与 Gemini 官方 API、自定义命令适配器。
- 写入了内置图像工具的调用协议：先把源图显示到对话中成为可见的编辑目标，再发起生成，prompt 声明以刚显示的图为唯一编辑对象；生成结果从工具的落盘目录取回。内置工具的参考图通过对话上下文传递，参数表里没有参考图选项属于正常现象，不构成回退到脚本后端的理由。该协议借鉴自 GordenImage2PPTX。
- 新增 `.env` 密钥配置：复制 `env.example` 填入可用密钥即可，脚本自动加载，系统环境变量优先，`.env` 不入库。
- 新增 `generate_clean_plate.py`（脚本后端适配器，带 `--precheck` 自检）和 `prepare_clean_plate_mask.py`（生成提示用的掩膜与叠加图准备）。

### 新增：质量验证增强

- 像素级视觉对比（`visual_compare_qa.py`）：重建结果与原图逐块比对，输出并排图、混合图、差异热力图和分块评分，组装时自动运行，高差异区块作为复查触发器进入质量报告。
- 文本层策略文档：界定压在连续视觉场上的文字、代码和公式如何分层与验收。
- 分层系统新增 `icons` 层，解决图片资产被填充面板遮挡的绘制次序问题，明确八层绘制顺序。

### 修复与维护

- 兼容 OpenCV 5：`HoughLinesP` 返回形状在 5.x 中有变化，结构检测按新旧两种形状统一处理，4.x 与 5.x 均可用。
- 移除四个已被取代的遗留脚本（旧版一键管线、cairosvg 预览渲染、资产决策审计、生成图下载工具），预览渲染统一走 Chrome。
- `requirements.txt` 逐项标注用途，说明 cairosvg 为可选依赖，缺失时自动回退 svglib。
- SKILL.md 参考文档清单与脚本清单补全，内部辅助脚本单独标注不直接运行。

---

## 0.1.0

首个公开版本。混合重建策略：结构重绘为矢量、文字重打为文本、公式重建为 LaTeX 并导出可编辑 Office Math、来源特异视觉内容裁切为图片资产；输出可编辑 SVG、内嵌资产 SVG 与原生 PPTX；PaddleOCR（PP-OCRv6）与 OpenCV 提供测量证据；manifest 驱动全流程可复现；质量审计覆盖结构缺失、文本困陷、公式转换与可编辑性。
